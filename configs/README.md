# Configs

Configuration files in this directory are for ADK-Go agent runtime wiring and MCP endpoint configuration.

## Files

- `mcp-endpoints.env.example`: environment variables for MCP server connectivity.
- `agent-runtime.md`: runtime contract for orchestrator and executor agents.

No standalone APIs or microservices are introduced by this foundation.
