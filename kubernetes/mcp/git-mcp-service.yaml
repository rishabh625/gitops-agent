apiVersion: v1
kind: Service
metadata:
  name: git-mcp-server
  namespace: ${namespace}
  labels:
    app.kubernetes.io/name: git-mcp-server
    app.kubernetes.io/part-of: mcp
spec:
  type: ${service_type}
  selector:
    app.kubernetes.io/name: git-mcp-server
  ports:
    - name: http
      port: ${git_mcp_port}
      targetPort: http
