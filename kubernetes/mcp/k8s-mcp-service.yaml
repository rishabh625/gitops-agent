apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-server
  namespace: ${namespace}
  labels:
    app.kubernetes.io/name: k8s-mcp-server
    app.kubernetes.io/part-of: mcp
spec:
  type: ${service_type}
  selector:
    app.kubernetes.io/name: k8s-mcp-server
  ports:
    - name: http
      port: ${k8s_mcp_port}
      targetPort: http
