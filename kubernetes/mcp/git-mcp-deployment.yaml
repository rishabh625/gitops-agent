apiVersion: apps/v1
kind: Deployment
metadata:
  name: git-mcp-server
  namespace: ${namespace}
  labels:
    app.kubernetes.io/name: git-mcp-server
    app.kubernetes.io/part-of: mcp
spec:
  replicas: ${git_mcp_replicas}
  selector:
    matchLabels:
      app.kubernetes.io/name: git-mcp-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: git-mcp-server
        app.kubernetes.io/part-of: mcp
    spec:
      serviceAccountName: ${service_account_name}
      containers:
        - name: git-mcp
          image: ${git_mcp_image}
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: git-mcp-config
            - secretRef:
                name: git-mcp-secret
          ports:
            - name: http
              containerPort: ${git_mcp_port}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
